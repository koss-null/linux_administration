-better "block ratings" visualisation
-working block preview (depth-2 bastardizer)
-porting to other interfaces (bastet is a choosing algorithm, after all, it can be applied virtually to every tet*is clone)
